{
  "name": "work-dashboard",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "tsc && cp dist/app.js app.js && cp dist/sw.js sw.js",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}


