{
  "name": "work-dashboard",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "tsc && node scripts/postbuild.js",
    "watch": "tsc -w",
    "start": "http-server -c-1 -p 8000 -a 127.0.0.1 ."
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "http-server": "^14.1.1"
  }
}
